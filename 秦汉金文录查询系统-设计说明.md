# 秦汉金文数据库 — 设计说明文档

## 1. 概述

本项目集成《秦汉金文录》(容庚) 与《秦汉金文汇编》(孙慰祖、徐谷富) 两大金文资料，生成一个可离线使用的静态查询网页。用户可通过浏览器跨数据集统一检索器名、铭文和著录信息。

### 文件清单

| 文件 | 说明 | 大小 |
|------|------|------|
| `秦汉金文数据库.html` | **主查询网页**（单文件，内嵌全部 1623 条数据） | ~339 KB |
| `秦汉金文录查询.html` | 旧版单数据集网页（《秦汉金文录》835条，保留备用） | ~283 KB |
| `秦汉金文录数据.json` | 《秦汉金文录》结构化数据（835条） | ~328 KB |
| `秦汉金文汇编数据.json` | 《秦汉金文汇编》结构化数据（788条） | ~283 KB |
| `秦汉金文录 修改——康博文师兄给的原版.docx` | 《秦汉金文录》原始文档 | - |
| `merge_image_names.py` | 把导出的图片名称记录 JSON 合并回主 HTML 的脚本 | 3 KB |
| `batch_extraction_results.txt` | 《秦汉金文汇编》上编扫描页文字 | ~86 KB |
| `batch_extraction_results_2.txt` | 《秦汉金文汇编》下编扫描页文字 | ~146 KB |

---

## 2. 数据集结构

网页含 **三个独立数据集**，可分开查看或全部合并通检。

### 数据集 1：《秦汉金文录》(金文录)

来源：康博文整理的容庚《秦汉金文录》Word 文档。**835 条**。

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | int | 序号（★1 ~ ★835） | 1 |
| `source` | str | 出处及页码 | "秦金文錄P111.1" |
| `name` | str | 器名 | "廿六年詔權" |
| `inscription_full` | str | 铭文全文 | |
| `notes_full` | str | 著录/备注全文 | |
| `image_name` | str | **图片名称**（默认空，可手动编辑并保存） | "权1-正面图.jpg" |

- **秦金文**：86 条；**汉金文**：749 条

### 数据集 2：《秦汉金文汇编·上编》(汇编上)

来源：`batch_extraction_results.txt`（qwen3.7-plus 识别的扫描页文字）。**400 条**。

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | int | 序号 | 1 |
| `dataset` | str | "上编" | |
| `tif_file` | str | 原始 tif 文件名 | "秦汉金文汇编正文上编001.tif" |
| `page` | int | 原书页号 | 1 |
| `name` | str | 识别出的器名 | "新郪兵符" |
| `text` | str | 该页全文（器名+铭文） | |
| `source` | str | "秦汉金文汇编·上编·001" | |

### 数据集 3：《秦汉金文汇编·下编》(汇编下)

来源：`batch_extraction_results_2.txt`。**388 条**。字段同上编，`tif_file` 如"秦汉金文汇编正文下编001.tif"。

- 主要为《秦漢金文字匯》卷一至卷十五的字头索引（按部首/字头排列的器名罗列）
- 另含"主要引用參考書目"、"後記"等全书附录文字

---

## 3. 技术架构

### 整体方案：纯静态单页应用

- **零依赖**：无框架、无构建工具、无后端服务
- **单文件部署**：三个数据集全部内嵌于一个 HTML 中，双击浏览器即用
- **离线可用**：无需网络连接

### 前端

| 层次 | 技术 |
|------|------|
| 样式 | 纯 CSS（无框架），仿古籍风格配色 |
| 逻辑 | 原生 JavaScript（ES6+） |
| 数据 | JSON 内嵌于 HTML `<script>` 标签中 |
| 搜索 | 客户端全文检索，多关键词 AND 匹配 |

### 数据流

```
《秦汉金文录》docx ─┐
                    ├─→ Python 解析脚本 ─→ JSON ─→ 内嵌至 HTML
《秦汉金文汇编》txt ─┘   (parse_docx / parse_huibian)
```

---

## 4. 功能说明

### 数据集切换

页头有四个数据集切换按钮：

| 按钮 | 数据范围 | 条数 |
|------|---------|------|
| **秦汉金文录** | 仅金文录 | 835 |
| **汇编·上编** | 仅汇编上编 | 400 |
| **汇编·下编** | 仅汇编下编 | 388 |
| **全部合并** | 三个数据集总和 | 1623 |

切换后搜索结果自动限定在所选数据集范围。

### 搜索功能

- **全文检索**：匹配器名、出处、铭文/著录（或汇编页全文）所有字段
- **多关键词**：空格分隔，要求全部命中（AND 逻辑）
  - 例：`廿六年 權`
- **实时搜索**：输入即出结果
- **高亮显示**：匹配关键词黄色高亮

### 结果展示

每条结果卡片包含：序号标签、数据集标签（金文录/汇编上/汇编下）、出处、**tif 文件名（汇编上/下编特有）**、器名、正文（铭文/著录/页全文）。

**tif 文件名显示与检索**：
- 汇编·上编/下编的每条结果都会显示对应 tif 文件名（如 `📄 秦汉金文汇编正文上编001.tif`），方便对照原扫描页
- tif 文件名也参与全文搜索，直接输入 `上编180`、`下编100` 等即可定位到指定页

### 图片名称编辑与保存（金文录·专用）

**秦汉金文录** 的每条结果在其**卡片顶部 header 行**（紧挨「★序号｜数据集标签｜出处」）有一个「图片名称」输入框，与条目标题行对齐：

- **字段**：数据内嵌 `image_name` 字段，默认空
- **编辑展示**：输入框前不显示图标；**只有进入编辑状态（输入框内容发生改动）时才显示「保存」按钮**，保存成功后按钮自动收起，与已保存内容一致时按钮隐藏
- **编辑并保存单条**：输入图片名称，点「保存」即写入浏览器 localStorage（键 `jwl_image_names`）
- **一键保存全部**：右下角「💾」浮动按钮（位于「↑ 返回顶端」按钮左侧），把**当前画面所有输入框的现值**一次性写入 localStorage（未点单条保存的值也会被写入），并提示本次修改条数。注意：仅保存当前 DOM 中已渲染出来的输入框，因视图切换而丢失的值不属于保护范围（如需要防丢可另用单条实时保存）
- **持久化**：localStorage 保存在当前浏览器，刷新/重开页面不丢失（换浏览器或换电脑需导入）
- **检索**：图片名称也参与全文搜索，输入图片名可定位到对应条目
- **导出**：页头「⬇ 导出图片名称填写记录」按钮，把已填写的记录下载为 JSON（含 id、source、name、image_name）
- **导入**：页头「⬆ 导入图片名称记录」按钮，把之前导出的 JSON 合并回来（用于换设备/换浏览器恢复）

### 返回顶端

页面右下角有半透明圆形「↑」浮动按钮，向下滚动超过 300px 时淡入显示，点击即平滑滚回页面顶部。「💾 一键保存全部」浮动按钮并排显示在其左侧，滚动到结果区时同时浮现。

**合并回主文件（可选，一劳永逸）**：
导出的 JSON 可用附带的 `merge_image_names.py` 脚本写入 HTML 内嵌数据，之后即使换设备 image_name 也在：
```
python3 merge_image_names.py 秦汉金文录_图片名称记录.json
```
脚本会先自动备份原 HTML。

---

## 5. 设计决策说明

### 为什么数据全部内嵌而非 fetch？

- `file://` 协议下 `fetch()` 受 CORS 限制无法工作
- 内嵌后单文件即用，双击 HTML 即可，无需服务器，无需额外文件

### 数据量评估

- 三数据集共 1623 条，内嵌 JSON 约 32 万字符，单 HTML 约 339 KB
- 对现代浏览器完全无压力，离线可流畅运行

### 为什么三个数据集分开而非强行统一？

- 三者结构不同：《金文录》是"★条目"（器名+铭文+著录清析）；《汇编》上编是"页"（器名+铭文）；下编是"字头索引"（器名罗列）
- 分开保留各自原始结构，避免信息扭曲
- "全部合并"满足跨书通检需求，通过数据集标签区分来源

### 搜索为什么用 AND 而非 OR？

- AND 能精确缩小范围，符合学术检索习惯
- 需 OR 效果时，单次搜一个词即可

---

## 6. 使用方式

1. **打开网页**：双击 `秦汉金文数据库.html`
2. **选数据集**：点页头按钮切换检索范围
3. **搜索**：输入关键词实时检索
4. **多关键词**：空格分隔，同时匹配多条件

---

## 7. 局限性

- 《秦汉金文汇编》上下编的文字来自扫描页 OCR，**存在少量识别错字/乱码**，且下编为字头索引结构，器名判定为智能识别（第一候选），个别页可能不准
- 《金文录》数据解析基于缩进规则，极少数条目分类可能不精确
- 稀有 Unicode 字符（如 𤫊、𨜏 等）部分字体可能无法正确渲染
- 空搜索显示全部 1623 条时，首屏渲染可能有短暂延迟

---

*文档版本：v2.3 · 2026-08-31 · 图片名称去掉图标、仅编辑态显示保存按钮；「一键保存全部」移到右下角与「返回顶端」并排*
