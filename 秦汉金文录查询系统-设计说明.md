# 秦汉金文数据库 — 设计说明文档

## 1. 概述

本项目集成《秦汉金文录》(容庚) 与《秦汉金文汇编》(孙慰祖、徐谷富) 两大金文资料，并在 v2.2 起续增《汉代铜镜铭文暨图像集成》(鹏宇编) 扫描图文字，于 **v2.5 统一为「十卷一套实现」**：卷一/二/三/六/七/八/九/十/十八/二十 共 10 卷全部采用同一数据 schema、同一图像渲染、同一可视化切卷开关。用户可通过浏览器跨数据集统一检索器名、铭文、著录信息与铜镜镜铭原文。**v2.6** 起在每条卡片上加入「补注释文 / 自加字段」编辑层（localStorage 覆盖层，可导出/导入 JSON 漫游），用于个人复核补录，详见第 9 章。**v2.8** 加入「检索范围」三选一（全部／器名／释文）；**v2.9** 完成四项零感知性能优化；**v3.0** 起支持**全部字段**逐条编辑（除 `id`），见第 10 章；**v3.1** 收窄「释文」检索范围至铭文正文字段；**v3.2** 将著录备注 `notes_full` 也移出「释文」检索；**v3.3** 把 v2.6 补注释文覆盖层的文本并入「释文」检索。

### 文件清单

| 文件 | 说明 | 大小 |
|------|------|------|
| `秦汉金文数据库.html` | **主查询网页**（单文件，内嵌金文三大库 + 汉代铜镜铭文十卷全数据） | ~2.8 MB |
| `秦汉金文录查询.html` | 旧版单数据集网页（《秦汉金文录》835条，保留备用） | ~283 KB |
| `秦汉金文录数据.json` | 《秦汉金文录》结构化数据（835条） | ~328 KB |
| `秦汉金文汇编数据.json` | 《秦汉金文汇编》结构化数据（788条） | ~283 KB |
| `秦汉金文录 修改——康博文师兄给的原版.docx` | 《秦汉金文录》原始文档 | - |
| `merge_image_names.py` | 把导出的图片名称记录 JSON 合并回主 HTML 的脚本 | 3 KB |
| `build_jingwen.py` | 【v2.2 卷八专用·已废弃】卷八首版数据构建脚本（v2.5 已并入 `build_mirror.py`） | - |
| `build_mirror.py` | **汉代铜镜铭文十卷（v2.5）统一数据构建脚本**（详第 8 章）；输出每卷精简 JS 与全集 | - |
| `inject_mirror_v25.py` | 把 build_mirror 生成的各卷 `DATA_JW…` 常量与渲染逻辑注入 `秦汉金文数据库.html` 的脚本 | - |
| `inject_overlay_v26.py` | **v2.6** 注入「补注释文/自加字段」覆盖层（CSS+JS+事件委托+导出导入）到主 HTML 的脚本 | 5 KB |
| `inject_extra_v27.py` | **v2.7** 注入「在当前库新增本人条目」（浮动按钮＋模态表单＋高位合成 id＋删除）脚本 | 7 KB |
| `inject_field_overlay_v30.py` | **v3.0** 注入「全字段逐条编辑」面板（FIELDOV 字段级覆盖层＋导出导入＋清除）脚本 | 待生成 |
| `batch_extraction_results.txt` | 《秦汉金文汇编》上编扫描页文字 | ~86 KB |
| `batch_extraction_results_2.txt` | 《秦汉金文汇编》下编扫描页文字 | ~146 KB |
| `../2025-汉代铜镜铭文暨图像集成/…` | 铜镜铭文各卷 OCR 提取文字页 JSON（`output_all/…NN-鹏宇/page_*.json`），附图在 `database/` 下 `汉代…图像集成{卷}(…)灰/` 目录 | - |

---

## 2. 数据集结构

网页含 **三个独立数据集**，可分开查看或全部合并通检。

### 数据集 1：《秦汉金文录》(金文录)

来源：康博文整理的容庚《秦汉金文录》Word 文档。**835 条**。

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | int | 序号（★1 ~ ★835） | 1 |
| `source` | str | 出处及页码 | "秦金文錄P111.1" |
| `name` | str | 器名 | "廿六年詔權" |
| `inscription_full` | str | 铭文全文 | |
| `notes_full` | str | 著录/备注全文 | |
| `image_name` | str | **图片名称**（默认空，可手动编辑并保存） | "权1-正面图.jpg" |

- **秦金文**：86 条；**汉金文**：749 条

### 数据集 2：《秦汉金文汇编·上编》(汇编上)

来源：`batch_extraction_results.txt`（qwen3.7-plus 识别的扫描页文字）。**400 条**。

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | int | 序号 | 1 |
| `dataset` | str | "上编" | |
| `tif_file` | str | 原始 tif 文件名 | "秦汉金文汇编正文上编001.tif" |
| `page` | int | 原书页号 | 1 |
| `name` | str | 识别出的器名 | "新郪兵符" |
| `text` | str | 该页全文（器名+铭文） | |
| `source` | str | "秦汉金文汇编·上编·001" | |

### 数据集 3：《秦汉金文汇编·下编》(汇编下)

来源：`batch_extraction_results_2.txt`。**388 条**。字段同上编，`tif_file` 如"秦汉金文汇编正文下编001.tif"。

- 主要为《秦漢金文字匯》卷一至卷十五的字头索引（按部首/字头排列的器名罗列）
- 另含"主要引用參考書目"、"後記"等全书附录文字

### 数据集 4 起《汉代铜镜铭文暨图像集成》(鹏宇) — 十卷拆分 + 全集（v2.5 统一）

来源：`../2025-汉代铜镜铭文暨图像集成/`（鹏宇编，上海书画出版社）的逐 PDF 页 OCR 提取文字（`output_all/`），配以 `database/` 下各卷同名扫描图（`.tif`）。**十卷各自成一条数据集**：卷一/二/三/六/七/八/九/十/十八/二十 各一条 + 「汉代铜镜铭文·全集」`jw_all` + 「全部合并」`all`。** 每条记录对应一页正文图册上的一枚镜铭。**十卷同为下述精简 5 字段 schema**（不再用 v2.4 图纸里曾设想的 detail 字段化拆分；详情见文末第 8 章）：

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | int | 卷内递增流水号 | 1 |
| `source` | str | 著録引用（`text` 内"著録…"去"著録"前缀） | 《六安》圖124 |
| `name` | str | 器名 / 镜铭主题标题（可为空） | 「吾作佳鏡」銘羽人瑞獸博局紋鏡 |
| `text` | str | 该页全文（釋文/特徵/尺寸/出土地/著録…整段原文），供搜索 | （见下） |
| `image_name` | str | **含自身图目录相对 `database/` 前缀的完整 tif 路径**，脚本预填 | `汉代铜镜铭文暨图像集成二十（467灰）/汉代铜镜铭文暨图像集成二十-000013-zhengwen0003.tif` |

**`image_name` 由脚本按该卷真实图目录预填**（非手填，仅供展示 + 点击打开），结构细节与逐卷图文映射见第 8 章。

---

## 3. 技术架构

### 整体方案：纯静态单页应用

- **零依赖**：无框架、无构建工具、无后端服务
- **单文件部署**：《秦汉金文录》《秦汉金文汇编》《汉代铜镜铭文暨图像集成》等数据集全部内嵌于一个 HTML 中，双击浏览器即用
- **离线可用**：无需网络连接

### 前端

| 层次 | 技术 |
|------|------|
| 样式 | 纯 CSS（无框架），仿古籍风格配色 |
| 逻辑 | 原生 JavaScript（ES6+） |
| 数据 | JSON 内嵌于 HTML `<script>` 标签中 |
| 搜索 | 客户端全文检索，多关键词 AND 匹配 |

### 数据流

```
《秦汉金文录》docx ─┐
                    ├─→ Python 解析脚本 ─→ JSON ─→ 内嵌至 HTML
《秦汉金文汇编》txt ─┘   (parse_docx / parse_huibian)
```

---

## 4. 功能说明

### 数据集切换

页头数据集切换按钮分两组：

**金文类（三个独立数据集）：**

| 按钮 | 数据范围 |
|------|---------|
| **秦汉金文录** | 仅金文录（835） |
| **汇编·上编** | 仅汇编上编（400） |
| **汇编·下编** | 仅汇编下编（388） |

**汉代铜镜铭文（十卷）：**

十卷（一/二/三/六/七/八/九/十/十八/二十）**各有一个独立按钮**，另有一个「🔴 汉代铜镜铭文·全集」`jw_all` 合并全部 10 卷。按钮文字使用原本卷次编号「汉代铜镜铭文·卷一」…「…·卷二十」。当前实现中各按钮条数：卷一143/卷二257/卷三361/卷六239/卷七259/卷八386/卷九239/卷十271/卷十八342/卷二十393（合计 2890）。

**全部合并：** 三个金文数据集 + 铜镜铭文全集（10 卷）的总和 = 4513 条（跨库通检）。

切换后搜索结果自动限定在所选数据集范围。

### 搜索功能

- **检索范围可选（v2.8，v3.1/v3.2 两度收窄释文范围）**：搜索框左侧有三个切换按钮「**全部**／**器名**／**释文**」，决定关键词在哪些字段中匹配：

  | 按钮 | 匹配字段 | 用途 |
  |------|---------|------|
  | **全部**（默认） | 器名＋出处＋铭文/著录/页全文＋tif 图名＋图片名称＋补注覆盖层 | 原 v2.7 行为 |
  | **器名** | 仅 `name`（＋本人新增条目的器名、覆盖层自加字段） | 按器名精确找器，排除铭文中同字干扰 |
  | **释文** | **仅** `text`／`inscription_full`（铭文正文）＋补注释文覆盖层 `t`，**不含著录备注 `notes_full`、自加字段、器名、`source`、`tif_file`、`image_name`** | 按铭文找器（含本人补注版） |

  - 切换按钮即时生效（重跑当前检索），输入框占位提示随模式变化；统计行显示当前模式
  - **性能优化（v2.9）**：输入框为空时切换模式不重绘 DOM（三模式结果必然相同，均等于当前数据集全部条目），仅刷新统计行；实测点击耗时由 ~342ms 降至 ~3ms
  - **释文范围收窄（v3.1）**：原「释文·正文」把 `source`（著录出处）、`tif_file`（图号）、`image_name`（图片名称）一并纳入，导致搜「秦金文錄」在释文范围命中 86 条（该词只出现在出处里，与释文无关）。v3.1 撤掉这三项，释文范围只留铭文正文字段，同时把统计行/占位提示/按钮 title 的「释文·正文」统一改为「释文」
  - **`notes_full` 移出释文（v3.2）**：金文录的「陶四‧三十 猗文 陶作秦銅權十四」这类著录备注不再参与「释文」检索（v3.1 及以前按本人约定计入）。搜「陶作秦銅權」释文 **0 条**（v3.2 前为 17 条，全部来自备注）。备注仍照常显示在卡片正文并参与「全部」模式，仅不在「释文」模式匹配
  - **补注释文并入释文（v3.3）**：v2.6 抽屉补录的释文覆盖文本 `t` 此前只在「全部／器名」模式可搜（`getBodySearchText` 未拼覆盖层），v3.3 新增 `olText(e)`（仅取 `t`）拼入释文检索——补注过的释文在「释文」模式也能搜到，且**原正文仍同时可搜**（两者并集）。**自加字段不进「释文」模式**（仍只在「全部／器名」）；v3.0 字段编辑与 v2.7 新增条目经 `collect` 漏斗生效，本就参与三模式，不受影响
  - 例：搜「權」在「全部」183 条、「器名」72 条、「释文」126 条（v3.2 前为 155，差额 29 条均来自著录备注）；搜「秦金文錄」在「全部」86 条、「释文」**0 条**（v3.1 前为 86，可见范围已收窄）；金文录 835 条范围内「詔權」器名 26 条、释文 0 条
- **全文检索**：匹配器名、出处、铭文/著录（或汇编页全文）所有字段
- **多关键词**：空格分隔，要求全部命中（AND 逻辑）
  - 例：`廿六年 權`
- **实时搜索**：输入即出结果
  - **性能优化（v2.9）**：`onSearch` 加 150ms 防抖，快速连打/连点只执行最后一次检索，避免重复重绘
- **高亮显示**：匹配关键词黄色高亮（`.highlight`）
- **结果计数**：`在「器名」中找到 N 条匹配结果`，模式一目了然

### 性能实现要点（v2.9）

| 项 | 优化前 | 优化后 | 说明 |
|----|--------|--------|------|
| A 空输入切模式 | 每次重绘全部 4513 条（~342ms） | 仅刷新统计行（~3ms） | `setSearchMode()` 检测输入为空则直接返回 |
| B 输入防抖 | 每次按键都检索 | 150ms 防抖 | `onSearch()` 延迟调度，实际执行体为 `doSearch()` |
| C `esc()` 转义 | 每条 `createElement('div')` + `innerHTML` | 纯字符串 `replace` ＋ 查找表 | 顺序须先替 `&`，否则二次转义 |
| D `imgStore` | 每次 `get` 都 `JSON.parse` 全表 | 内存缓存，`save()` 同步缓存 | 写入仍全部走 `set()`/`save()`，缓存不会失效 |

- 统计行刷新统一走 `updateSearchStats()`（模式切换、检索、切库、初始化共用）
- 计数器 `.entry`（每条结果卡片），非 `.card`
- 优化后检索计数与优化前逐项一致（詔權 全部66/器名29/释文40；秦金文錄 释文86/器名0；權 全部183/器名72/释文155），无行为变化

### 结果展示

每条结果卡片包含：序号标签、数据集标签（金文录/汇编上/汇编下/汉代铜镜铭文·卷X）、出处、**tif 文件名（汇编上/下编特有）**、器名、正文（铭文/著录/页全文）。

**tif 文件名显示与检索：**
- 汇编·上编/下编的每条结果都会显示对应 tif 文件名（如 `📄 秦汉金文汇编正文上编001.tif`），方便对照原扫描页
- tif 文件名也参与全文搜索，直接输入 `上编180`、`下编100` 等即可定位到指定页

**铜镜铭文可点图（汉代铜镜铭文·各卷）：**
- 铜镜各卷的每条结果在卡片顶部 header 行显示一个可点击的图名链接 `🖼 汉代镜铭文暨图像集成八-000012-zhengwen0003.tif`
- 点击该链接，用系统的默认看图器（macOS 建议 **Safari** 打开页面）打开对应的原始扫描 tif 大图；因浏览器不原生渲染 tif，请用 Safari 打开（Chrome/Edge 会触发下载而非跳转到看图器）
- 图名也参与全文搜索（可输入 `zhengwen0003` 等定位）
- 图名由脚本按文末《图文映射》公式预填，对应关系客观来自页码，无需手填


### 图片名称编辑与保存（金文录·专用）

**秦汉金文录** 的每条结果在其**卡片顶部 header 行**（紧挨「★序号｜数据集标签｜出处」）有一个「图片名称」输入框，与条目标题行对齐：

- **字段**：数据内嵌 `image_name` 字段，默认空
- **编辑展示**：输入框前不显示图标；**只有进入编辑状态（输入框内容发生改动）时才显示「保存」按钮**，保存成功后按钮自动收起，与已保存内容一致时按钮隐藏
- **编辑并保存单条**：输入图片名称，点「保存」即写入浏览器 localStorage（键 `jwl_image_names`）
- **一键保存全部**：右下角「💾」浮动按钮（位于「↑ 返回顶端」按钮左侧），把**当前画面所有输入框的现值**一次性写入 localStorage（未点单条保存的值也会被写入），并提示本次修改条数。注意：仅保存当前 DOM 中已渲染出来的输入框，因视图切换而丢失的值不属于保护范围（如需要防丢可另用单条实时保存）
- **持久化**：localStorage 保存在当前浏览器，刷新/重开页面不丢失（换浏览器或换电脑需导入）
- **检索**：图片名称也参与全文搜索，输入图片名可定位到对应条目
- **导出**：页头「⬇ 导出图片名称填写记录」按钮，把已填写的记录下载为 JSON（含 id、source、name、image_name）
- **导入**：页头「⬆ 导入图片名称记录」按钮，把之前导出的 JSON 合并回来（用于换设备/换浏览器恢复）

### 返回顶端

页面右下角有半透明圆形「↑」浮动按钮，向下滚动超过 300px 时淡入显示，点击即平滑滚回页面顶部。「💾 一键保存全部」浮动按钮并排显示在其左侧，滚动到结果区时同时浮现。

**合并回主文件（可选，一劳永逸）**：
导出的 JSON 可用附带的 `merge_image_names.py` 脚本写入 HTML 内嵌数据，之后即使换设备 image_name 也在：
```
python3 merge_image_names.py 秦汉金文录_图片名称记录.json
```
脚本会先自动备份原 HTML。

---

## 5. 设计决策说明

### 为什么数据全部内嵌而非 fetch？

- `file://` 协议下 `fetch()` 受 CORS 限制无法工作
- 内嵌后单文件即用，双击 HTML 即可，无需服务器，无需额外文件

### 数据量评估

- 金文三数据集约 1623 条（金文录835 + 汇编上400 + 汇编下388）
- 汉代铜镜铭文 10 卷合计 2890 条（卷一143/二257/三361/六239/七259/八386/九239/十271/十八342/二十393）
- 单 HTML 体积随数据集增多相应增长（现约 2.8 MB），现代浏览器加载仍流畅（铜镜各卷 data 仅存字段化文字，不含 tif 图像本体）

### 为什么三个数据集分开而非强行统一？

- 三者结构不同：《金文录》是"★条目"（器名+铭文+著录清析）；《汇编》上编是"页"（器名+铭文）；下编是"字头索引"（器名罗列）
- 分开保留各自原始结构，避免信息扭曲
- "全部合并"满足跨书通检需求，通过数据集标签区分来源

### 为什么汉代铜镜铭文要按卷拆分 + 提供全集？

- 铜镜各卷内容相互独立（每卷一个 PDF、对应一批扫描图），按卷拆成独立数据集便于对准某卷的图版逐卷查
- 另设「铜镜铭文全集」用于跨卷合并检索整套镜铭
- 镜铭文字同样来自逐 PDF 页 OCR，字段经规范化，释文内容按"乙方案"原样保留、不臆改（同源镜像铭）

### 搜索为什么用 AND 而非 OR？

- AND 能精确缩小范围，符合学术检索习惯
- 需 OR 效果时，单次搜一个词即可

---

## 6. 使用方式

1. **打开网页**：双击 `秦汉金文数据库.html`
2. **选数据集**：点页头按钮切换检索范围
3. **搜索**：输入关键词实时检索
4. **多关键词**：空格分隔，同时匹配多条件

---

## 7. 局限性

- 《秦汉金文汇编》上下编的文字来自扫描页 OCR，**存在少量识别错字/乱码**，且下编为字头索引结构，器名判定为智能识别（第一候选），个别页可能不准
- 《金文录》数据解析基于缩进规则，极少数条目分类可能不精确
- 稀有 Unicode 字符（如 𤫊、𨜏 等）部分字体可能无法正确渲染
- 空搜索显示全部数据条时，首屏渲染可能有短暂延迟
- 汉代铜镜铭文文字同样来自 OCR，个别字段（編號、著録名）存在漏识/错别；释文等正文按乙方案原样保留不改
- 铜镜 `.tif` 大图浏览器不原生渲染，需用 Safari（或下载后本地看图器）查看；Chrome/Edge 点击会触发下载

---

## 8. 汉代铜镜铭文（鹏宇）· 图文映射与十卷统一构建（v2.5）

### 8.1 素材与来源

- 书目：《汉代铜镜铭文暨图像集成》(鹏宇 编著，上海书画出版社)，**多册/多卷**，每卷有独立 PDF 与其对应扫描图。
- 文字（已 OCR 提取）：`../2025-汉代铜镜铭文暨图像集成/output_all/2025-汉代铜镜铭文暨图像集成NN-鹏宇/page_XXXX.json`（NN=卷次，逐 PDF 页一个 json，`content` 为该页 OCR 文本分块）。
- 图片：`database/` 下各卷目录内的 `.tif` 图（横向扫描图，已按正文页序命名）。

### 8.2 关键体例结论

这套书每卷正文图册与 OCR 文字页**逐页一一对应**（一页正文图像 ≈ 一页文字 OCR）。经实测，每卷在「正文窗」内 `page_XXXX.json` 页码**零缺失**、页数恰好等于该卷正文图数。因此可将文字条目与 `zhengwenNNNN.tif` **按页码严格配对**，无需人工逐件核对（但每卷窗的**起点/终点**需先由人工给定锚点校准，见 8.3）。

唯一例外：**卷二十**的文字提取缺前两张正文图（对应 zhengwen0001/0002 无文字 JSON），故卷二十的正文窗从 zhengwen0003 起、且只到 zhengwen0407（其后 zhengwen0408–0457 为卷末无文字/未提取的纯图区，不入库）。

### 8.3 逐卷正文窗（人工校准后的锚点）与映射公式

对每一卷设「锚点 PDF 页」`anchor_pdf`（该卷第一张正文图在文字 JSON 中的页码）与其对应的 `anchor_zw`（该页图在 `zhengwen` 序列中的序号）。则正文窗内任一文字页 `page` 对应 `zhengwen` 序号：

```
zhengwen序号 = page − anchor_pdf + anchor_zw
```

**十卷逐卷配置（v2.5 统一实现；下表含卷八重迁，均已人工锚点校准）：**

| 卷 | 数据集标签 | 文字窗 (page) | 图范围 (zhengwen) | anchor_pdf | anchor_zw | 覆盖 |
|---|---|---|---|---|---|---|
| 卷一 | 汉代铜镜铭文·卷一 | 0032–0179 | 0001–0148 | 32 | 1 | 148 页 |
| 卷二 | 汉代铜镜铭文·卷二 | 0018–0308 | 0001–0291 | 18 | 1 | 291 页 |
| 卷三 | 汉代铜镜铭文·卷三 | 0020–0404 | 0001–0385 | 20 | 1 | 385 页 |
| 卷六 | 汉代铜镜铭文·卷六 | 0018–0282 | 0001–0265 | 18 | 1 | 265 页 |
| 卷七 | 汉代铜镜铭文·卷七 | 0018–0282 | 0001–0265 | 18 | 1 | 265 页 |
| 卷八 | 汉代铜镜铭文·卷八 | 0022–0407 | 0003–0388 | 22 | 3 | 386 条 |
| 卷九 | 汉代铜镜铭文·卷九 | 0016–0272 | 0001–0257 | 16 | 1 | 257 页 |
| 卷十 | 汉代铜镜铭文·卷十 | 0018–0335 | 0001–0318 | 18 | 1 | 318 页 |
| 卷十八 | 汉代铜镜铭文·卷十八 | 0020–0390 | 0001–0371 | 20 | 1 | 371 页 |
| 卷二十(部分) | 汉代铜镜铭文·卷二十 | 0022–0426 | **0003–0407** | 22 | 3 | 405 页 |

> 卷二十 anchor_zw=3：因该卷文字提取缺前两张正文图（zhengwen0001/0002 无文字 JSON）。卷二十窗覆盖到文字 PDF 页 0426（= zhengwen0407），其后 zhengwen0408–0457 为无文字纯图区不入库。

**v2.5 —— 卷八亦迁入统一 schema**：卷八原为 v2.2 的独立实现（数据集键 `jingwen`、图名靠全局常量 `JINGWEN_IMG_BASE` 拼双层目录）。v2.5 把它与其余 9 卷**重迁移为同一套**：数据集键改为 `jw8`，`image_name` 一并改写为含自身目录的相对完整路径，删除 `DATA_JINGWEN` 常量与 `JINGWEN_IMG_BASE`，渲染不再有任何 `ds==='jingwen'` 特判。最终 10 卷（`jw1,jw2,jw3,jw6,jw7,jw8,jw9,jw10,jw18,jw20`）同构，总计 **2890 条**。

**写入 HTML 时**：每条镜像卷一个常量（`DATA_JW1`…`DATA_JW20`），卷标签沿用书籍本卷次编号；另设按钮「汉代铜镜铭文·全集」`jw_all` 通检 10 卷；`all` 全部合并 = 金文三大库（835+400+388）+ 铜镜全集 2890 = **4513 条**。判定某条目是否为铜镜/可点图，用 `MIRROR_KEYS.includes(ds)`（`ds = e._ds`），不再依赖任何数据集名特判。

### 8.4 image_name 生成（预填公式）

`image_name` 由该条 `zhengwen 序号 n` 依**该卷实际图文件名表**查出完整 tif 文件名。命名规律 `汉代{镜|铜镜}铭文暨图像集成{卷}-{书页号:06d}-zhengwen{n:04d}.tif`（书名「镜/铜镜」与书页号各卷不同，卷一另有 `*-xuN.tif` 序页）。构建时**读取 database 下该卷真实图目录**，按文件名解析（卷、zhengwen号）生成「序号→文件串」映射；**同时把每条 `image_name` 直接存为该卷 tif 相对 `database/` 的完整路径**（如同卷八所存的前缀+文件名），确保每条图名自带目录前缀、缺图即报错、杜绝拼错与跨卷共用全局前缀。

> 原因：各卷图目录前缀互不相同（如 `汉代镜铭文暨图像集成八…` vs `汉代镜铭文暨图像集成二…` vs `汉代铜镜铭文暨图像集成二十…`），HTML 不能像卷八那样靠单一 `JINGWEN_IMG_BASE` 常量承载所有文件名的目录段。

每条入库记录采用**继承卷八裁剪后的精简 5 字段 schema**：`id`(卷内递增序号)、`source`(=著録去前缀)、`name`(器名/主题)、`text`(=该页全文，含釋文/特徵/尺寸等整段)、`image_name`(含自身目录的相对完整 tif 路径)。所有卷字段完全一致，前端渲染/搜索只用这 5 键（不再有 bianhao/page 等卷八特例字段差异）。

### 8.5 构建脚本

`build_mirror.py` 一次构建全部 10 卷（基于 v2.2 `build_jingwen.py` 的解析逻辑参数化，新增 v2.5 卷表 `VOLS`、逐卷真实图目录解析、统一 schema 裁剪、每卷 JS 写出）。卷八也由同一 `VOLS` 循环统一构建（数据源即其卷 JSON），去除了原先经 `jingwen` 独立注入 HTML 的路径：

```
python3 build_mirror.py              # 读 output_all 各卷文字 + database 各卷图目录
                                     # 输出：/tmp/mirror_js/jw{1..20}.js（各卷精简 JS）
```
- 解析每卷正文窗内每页文字：拼接为整段 `text`（釋文/特徵/尺寸/…均保留页内原文）
- 得到 `image_name`（真实 tif 名，仅当该 tif 文件存在才有；不存在则该条标记缺图便于校验；v2.5 实测 10 卷缺图 = 0）
- 把每条 `image_name` 存为**含自身目录前缀的相对完整路径**（不再依赖单目录常量）
- 逐卷写出精简 5 字段 JS 数组

> 注：注入 HTML 用字符串 `.replace()`（不用 `re.sub()`），否则 `\n` 会被当转义替换成真实换行而破坏 JS。

### 8.6 写入 HTML

用 `inject_mirror_v25.py` 对 `秦汉金文数据库.html` 做手术式字符串替换（改前先备份）：
1. 插入各卷 `DATA_JW8` + `DATA_JW1/2/3/6/7/9/10/18/20` 数据常量（置于原 `DATA_JINGWEN` 位置并删除该旧常量）
2. `DATASETS` 登记 10 卷键 `jw1…jw20`；`TAGS` 各卷标签配色；定义 `MIRROR_KEYS`（十卷键数组）与 `jwName`（键→显示名，含 `jw_all`=全集）
3. 数据集按钮区分层：金文三大库按钮不动；铜镜区**每卷一个独立按钮**（label「汉代铜镜铭文·卷X」）+「🔴 汉代铜镜铭文·全集」`jw_all` +「⚫ 全部合并」`all`
4. `initCounts`(每卷计数)、`dsName`(显示名)、`getDataForCurrent`(全集 `jw_all`=集 10 卷；`all`=金文三库+10 卷)、`getSearchText`(含 image_name) 接入
5. `renderEntry` 对铜镜条目（`MIRROR_KEYS.includes(ds)` 且 `e.image_name`）把 `image_name` 渲染为可点 tif 链接：`<a class="img-link" href="<该条自带的含目录完整路径>" target="_blank">🖼 …</a>`；**删除 `JINGWEN_IMG_BASE` 常量与 `const jingInfo` 中的 `ds==='jingwen'` 特判**，统一走 `isMirror` 判定
6. `CSS` 补充 `.tag-jw8` 等各卷标签配色（复用既有 `.tag-*` 风格）

按钮交互复用通用 `.ds-btn` 点击绑定（`querySelectorAll('.ds-btn')`），新增按钮零额外绑定。

### 8.7 编辑与保存 & 浏览

- 铜镜各卷的 `image_name` 由脚本预填，**不参与 localStorage 手填编辑**（仅供展示 + 可点打开），与金文录的可手填 image_name 在逻辑上分开
- 图片打开走 `.img-link` 超链接：须用 **Safari** 打开页面，方能由系统以默认看图器显示 tif；Chrome/Edge 会触发下载（浏览器限制）

---

## 9. 个人复核覆盖层（v2.6：补注释文 / 自加字段）

**动机**：金文录字段富结构（`inscriptions`/`inscription_full`），而汇编与铜镜十卷的「释文」只是内嵌于整页 `text` 前段的标签内容，并非独立列。为让用户对**任何数据集、任何单条**做个人复核补录（改错释文、补考释、加备注），引入**普适档覆盖层**：对某条整体重置其 `text`（即将其视为「该条正文」覆盖），或给该条追加自加字段。

**关键边界**：单文件离线 HTML（`file://`）JS 无法把改动写回 HTML 自身，故覆盖层**只存浏览器 localStorage**，与既有金文录 image_name 手填同一套逃生逻辑（导出/导入 JSON 漫游）。**不改 DATA 原值**，可随时全量一键清空还原。

### 9.1 存储
- localStorage 键 `wbh_overlay_v26`（`ARCH_KEY`）
- 形状：`{ "<数据集短键>|<条目id>": { t?: "释文/正文覆盖文本", f?: {字段名: 值} } }`。空任务（无 t 且无任何 f）自动不落盘/删除该条。
- 短键 = 数据集 `_ds`：金文三库（`jinwenlu`《金文录》、`shang`/`xia`《汇编》上下编）＋铜镜卷 `jw1…jw20`；条目 id 为该库内编号。缓存经 `OL._cache`，`OL.reset()` 使外部 clear 生效。

### 9.2 顶层视图（随渲染注入）
- 每张卡片 `.ol-card`（`<details>` 抽屉）；summary 显示条目现状：无补录→「✏️ 补注释文」，有补录→「已补录释文 N字段」+临时徽标。
- 抽屉分两段：**释文段**（textarea 默认取当前生效文本；`存释文` 存覆盖，`查看原text` 可对比原文）；**字段段**（列出已有自加字段，可改/删；底部可加新字段名+值）。

### 9.3 行为（均已浏览器实测）
- **改释文（整体覆盖）**：保存即 `rerenderEntryByOl` 重画该条，正文立刻变为覆盖版；文本另参与检索。
- **存空 = 还原**：保存空文本会把该条 t 移除→正文回到原 `text`。
- **加/改/删字段**：存后字段以 `字段名：值` 行显示于卡片下方并**参与检索**；`✕` 删除（confirm）。
- **检索**：`getSearchText` 追加 `olSearch(e)`（覆盖 text + 字段名[空格]值）。实测「只存在于新增字段」的词可精确命中该条。**v3.3 起**覆盖 `t` 另经 `olText(e)`（仅取 `t`、不含自加字段）拼入 `getBodySearchText`，「释文」模式也能搜到补注版释文。
- 覆盖层的 `text`、字段值均为**新增检索 source**，命中即高亮。

### 9.4 导出 / 导入（跨浏览器漫游）
- 工具栏新增 `⬇ 导出补充记录（N）` 与 `⬆ 导入补充记录`；JSON 结构：`[{ds,id,text?,fields:[{name,value}]}]`（与 DATA 本体分离）。
- 导入合并进 localStorage 后整表重画；不同数据集条目编号即便相同也因 `_ds` 前缀互不冲突；本机清缓存后靠此 JSON 还原个人成果。

### 9.5 注入脚本（改动前先备份）
`inject_overlay_v26.py` 在 `秦汉金文数据库.html` 上做锚点手术：`</style>` 前插 CSS → `function collect(ds)` 前插整段 JS 模块（CSS 文案 + `inject_overlay_v26.py` 内 `JS_FULL` 来自 /tmp `arch_*.js`）→ `renderEntry` 内 `bodyText` 改 `olBody(e)`、文本框前插 `${olBlockHtml(e)}` → `getSearchText` 追 `olSearch(e)` → 工具栏补导出导入按钮 → init 注册 `bindArchEvents` 等监听。
> 锚点须全局唯一（`.count==1`），注入前备份源文件；文档与实现保持一致，勿在运行时图省事 `re.sub`（`\` 转义坑，见 8.6 脚注同）。

---

## 10. 当前库新增本人条目（v2.7）

**动机**：主人不止要在已有条目上补字段/改释文（v2.6），还要能**在当前数据集下整条新增**（新的考古发现、本人收集的镜铭/陶範金文等），与原有条目一并通检、可开图、计数正确。

沿用同一套「覆盖层」纪律：单文件离线 HTML 无法写回自身，故新条目存 **localStorage**，不改 DATA 原值，JSON 漫游可带走，随时可全量清空还原。

### 10.1 入口与表单（字段＝当前数据集初始化字段，与库 schema 一致）
- 浮动按钮 `＋`（`#btnAddEntry`，与「↑ 返回顶端」「💾 全部保存」同屏同组悬浮，右下 `right:140px`；滚动≥300px 随组显现，带红色小徽计数）。
- 点开模态 `#exBox`（免依赖层）：仅当当前是**具体库/卷**才可新增；若是「全部合并 all / 铜镜全集 jw_all」聚合视图则提示先进入具体库。
- 表单字段**按当前数据集初始化**，与渲染实际读取的键一一对应（保证存进去就能正常显示/开图/检索）：

| 数据集 | 表单条目 | 落到对象键 |
|---|---|---|
| 秦汉金文录 jinwenlu | 器名 / 释文·正文（可多行）/ 著录·来源 / 图片名称 | name / inscription_full / source / image_name |
| 汇编·上/下 shang·xia | 器名 / 正文（释文+特徵，多行）/ 著录·来源 / tif 文件名 | name / text / source / tif_file |
| 铜镜卷 jwN | 器名 / 正文（释文…，多行）/ 著录·来源 / 原图路径 | name / text / source / image_name |

文本框可留空（之后在卡片抽屉用 v2.6「补注释文」再补也通）；器名留空→显示「（无器名）」。每个字段名用库内 key，故 **renderEntry/getEntryText/检索零改动**。

### 10.2 存储与合成 id
- localStorage 键 `wbh_extras_v27`（`EX_VKEY`）＝ `{ 数据集短键: [ 条目对象, … ] }`；条目为上述「库 schema 的完整对象」。
- **高位合成 id**：`EX_BASE = 100000` 起、按库各自分配（每个库独立计数），永不与源数据（部类数千级）冲突，且稳定支撑 v2.6 覆盖层 `库|id`（新条也可补注/加字段）。经 `collect` 流并显示 `★100000…`。

### 10.3 数据流 & 计数（单一漏斗合并）
- `collect(ds)` 在原有 `DATASETS[ds]` 之后**追加**该库存的新条目（带 `_ds`）→ 出条/检索/图片判定（铜镜 `image_name`、汇编 `tif_file`、金文录图名）全部自动生效。
- `initCounts` 改读「原数组长 + `exN(ds)`（该库新增数）」：新增/删除即时反映到按钮计数；`jw_all`(全集)、`all`(全部合并) 因按库并集，自动含各卷新增 → 实测 金文录 835(+1)→836、卷六 239(+1)→240、全集 2891、全部合并 4515，删除后各自还原。
- 新条目卡片右上角照常自己的库 tag + `★id`，右下角多一个 **🗑（删除本人新增）**（`exDelChip` 仅当 `EXTRAS.isExtra(_ds,id)` 才渲染，源条目永无此钮）。

### 10.4 事件与注入脚本
- `bindExEvents()`：＋开模态、按当前 ds 建字段、`保存新增`→`exSaveNew`（归一化→`EXTRAS.setEntry`→`switchDataset` 重画）、`取消`/遮罩关、🗑 删除（confirm）。
- `inject_extra_v27.py` 锚点手术（改前备份 `_backup_v2p7_*`）：`</style>` 前插 CSS → `function collect` 前插 JS 模块→ collect 追加新条 → `initCounts` 换入含 `exN` 版 → renderEntry 头部插 `${exDelChip(e)}` → 返回顶端钮后加 `＋` 浮钮、脚本前加模态 DOM（含 modal 结构 `#exBox/#exH/#exNote/#exForm/#exSave/#exCancel`）→ onScroll 收纳浮钮 → init 注册 `bindExEvents`。
- 每锚点断言 `.count==1` 不满足即终止且不写盘；注入前后 `node --check`。

### 10.5 已实测清单（浏览器，Safari/Chrome 通）
- 金文录新增(rich 字段落 inscription_full)、铜镜卷六新增(落 text)、保存后计数即时 835→836 / 239→240
- all/jw_all 聚合视图含新条且 del-chip 只在这两条出现；4515/2891 计数一致
- 检索命中新条正文与字段；删除（confirm）后 localStorage 清库、计数归位 239/835
- reload 持久：刷新后 localStorage 仍在、按钮计数照旧含新增
- v2.6 补注覆盖层/自加字段/导出导入、image_name 图片名编辑等既有能力对新条目同样可用（未破坏）
- 全页面 `node --check` 通过、0 JS 报错

---

## 11. v3.0 全字段编辑（字段级覆盖层 FIELDOV）

> 第 10 章的编号被 v2.7「新增本人条目」占用（历史沿用），故 v3.0 顺延为第 11 章。

### 11.1 目标与边界
- 让**任意条目的任意字段**都能改（器名、出处、释文、页全文、tif 图名…），且随时可逐字段还原。
- **只读**：`id`（主键，改动会断裂 `库|id` 映射与图片名关链）；铜镜各卷 `image_name`（原图相对路径，改了会让「点击开图」失效，故在面板中仅以灰底只读行展示，需改动请用 v2.3 的图片名编辑入口）。
- **源数据永不改写**：所有编辑落在 localStorage，导出 HTML 时数据仍为原始版本。

### 11.2 存储结构
- localStorage 键 `wbh_field_overlay_v30`（`FO_VKEY`）：`{ "库短键|id": { 字段: 新值, … }, … }`
- **只存与源数据不同的字段**；值被改回原值（或在表单中清空）时该字段自动删除，整条无剩余字段时整条键删除 → 「还原」是自然结果而非额外操作。
- 与既有层的关系：**v2.6 的 `t` / `f`（补注释文、自加字段）保持独立**，本层只做「替换既有字段值」。二者互不干扰：v2.6 抽屉照旧可用，字段面板不读写 `t`。
- 内存缓存 `_cache` 与 localStorage 同步（`_flush()` 写盘），与 v2.9 的 `imgStore` 缓存策略一致。

### 11.3 数据流（单一漏斗）
- `collect(ds)` 在生成带 `_ds` 的条目后，先套 `foApply(entry, ds)`（= `{...e, ...FIELDOV.get(ds,e.id)}`）→ 检索（`getSearchText` 三范围）、渲染（`renderEntry`）、计数、聚合视图**全部自动吃到覆盖值**，各处零改动。
- 实测：把金文录 id=1 的 `name` 改为「廿六年詔權【改】」后，「器名」范围检索立即命中 1 条，改回原值后覆盖自动消失、检索不再命中。

### 11.4 交互
- 每条卡片内一个折叠面板：**🔧 编辑本条目字段**（标题右侧显示「改任意字段（器名/出处/释文/图号…）；改回原值即自动还原」），有改动时折叠标题与卡片头部分别显示 `已改 N`／`🔧N` 徽标。
- 面板内逐字段一行：左为字段名 + 状态标签（**原文** 灰 / **已改** 橙），右为输入框（正文类用 textarea）；铜镜多一行只读「原图路径」（**只读** 标签）。
- 两个按钮：**保存改动**（逐字段比对源数据，只写不同的，改回原值即删该字段覆盖）、**还原本条全部**（清空该条本层全部覆盖后重绘）。
- 工具栏三项：**⬇ 导出字段改动（N）** / **⬆ 导入字段改动** / **↺ 清除全部字段改动**（危险色，二次确认并提示先导出）。
- 导出 JSON 结构：`{ _type:"qinhan-jinwen-field-overlay", _version:"3.0", _exported, entries:[{ds,id,over:{…}}] }`；导入为**合并**模式（同库同条同字段以文件为准），兼容裸数组。导入后自动重绘当前库。

### 11.5 源码位置（主 HTML 内）
- `FO_VKEY` / `FIELDOV`（get/set/clearField/clearEntry/clearAll/count/countIn/has/exportList/importList）
- `FO_EDITABLE`（金文录/汇编上/下各字段表）、`FO_MIRROR_FIELDS`（铜镜卷通用）、`foFieldsFor`、`foIsReadonly`
- `foValueOf` / `foBlockHtml` / `foSaveEntry` / `foResetEntry` / `foChangedCount` / `rerenderEntryByFo` / `updateFoLabel`
- `exportFoJSON` / `importFoJSON`；`foBlockHtml` 已接入 `renderEntry`（在正文与 v2.6 抽屉之间）；事件委托挂在 `bindArchEvents`（`.fo-sv`/`.fo-rs`）；按钮绑定在 `init()`。

### 11.6 已实测清单（浏览器）
- 金文录：改 `name` → 保存 → 徽标 `🔧1`、标签「已改」、正文标签保持「原文」、v2.6 层未被写入；检索「器名」命中 1 条；改回原值 → 覆盖自动清空、徽标消失、标签回「原文」
- 多库写入：`jinwenlu|5.source`、`shang|10.text`、`jw3|7.name` 同时存在，localStorage 结构正确
- 导出 → 清空 → 导入 往返：3 条全部还原，导入后检索仍命中覆盖值
- 六个库类型的字段表：金文录 4 字段、汇编上/下 4 字段、铜镜各卷 3 字段 + 只读原图路径（`jw1/jw3/jw8/jw20` 均验证）
- 全部合并 4513 条面板全渲染；检索回归「詔權」全部 66 / 器名 29 / 释文 40，与 v2.9 基线一致；0 JS 报错
- `node --check`（抽取 script）通过

---

*本文档对应主文件 `秦汉金文数据库.html`；十卷统一实现的构建与注入参照 `build_mirror.py` 与 `inject_mirror_v25.py`，补注覆盖层参照 `inject_overlay_v26.py`，新增本人条目参照 `inject_extra_v27.py`，改动前先备份 HTML。*

*文档版本：v3.3 · 2026-09-18 · **补注释文并入释文检索**：新增 `olText(e)`（仅取 v2.6 覆盖层 `t`）拼入 `getBodySearchText`，「释文」模式可搜到补注过的释文（原正文仍同时可搜）；自加字段仍只在「全部／器名」模式。history：v3.2 释文检索再收窄（撤掉著录备注 `notes_full`，只留 `text`／`inscription_full`）；v3.1 释文检索范围收窄（撤掉 `source`／`tif_file`／`image_name`，措辞统一为「释文」，`notes_full` 当时按约定仍计入）；v3.0 全字段编辑（FIELDOV 字段级覆盖层：除 `id` 外所有字段可改、铜镜 `image_name` 只读、逐字段还原、导出/导入/清除）；v2.9 性能优化四项（A 空输入切模式不重绘、B 输入 150ms 防抖、C `esc()` 去 DOM 化、D `imgStore` 内存缓存）；v2.8 搜索范围选择（全部／器名／释文）；v2.7 当前库新增本人条目（浮动 ＋ 按库给初始化字段表、高位合成 id、删除 chip、计数/检索/聚合视图全通）；v2.6 补注释文/自加字段覆盖层＋本地＋JSON 漫游；v2.5 十卷化统一实现；v2.4 先行接入 9 卷；v2.3 图片名称编辑与一键保存；v2.2 纳入卷八）*
